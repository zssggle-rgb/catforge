# Issue 01 — Implement configurable Rule DSL Engine

Implement YAML/JSON rule loading, schema validation, expression evaluation, weighted scoring, confidence calculation, evidence collection, and tests.

Acceptance:
- Invalid rule file fails validation.
- Valid TV claim rule file executes against fixture features.
- Changing YAML threshold changes output without code changes.
