# CA-G08 65E7Q E2E 与 205 部署验证记录

验证时间：2026-07-08 19:09 CST

## 1. 任务边界

本记录只覆盖 CA-G08：竞品智能体消费已发布 M12D 后的端到端验证和 205 部署验证。

本任务未生成 M12D，未修改 M12D 锚点角色，未新增竞品算法。

## 2. 205 部署记录

目标环境：

- 服务器：`deploy@123.56.42.205`
- 应用目录：`/opt/catforge`
- Compose 文件：`docker-compose.cloud.yml`
- API 容器：`catforge-api-1`

执行方式：

- 由于本地工作区包含未提交任务链改动，且本轮明确不 stage/commit，没有使用要求 clean worktree 的 `scripts/sync-hotfix-205.sh`。
- 使用 `scripts/hotfix-api.sh dev` 将 `apps/api-server/app/` 热修复到 205，并重启 API 容器。
- 手工同步小奥 OpenClaw 文件到：
  - `/opt/catforge/tools/openclaw/skills/xiaoao-home-appliance-market-analysis/SKILL.md`
  - `/opt/catforge/tools/openclaw/agents/xiaoao-home-appliance-market-analyst/AGENTS.md`
  - `/home/deploy/.openclaw/plugin-skills/xiaoao-home-appliance-market-analysis/SKILL.md`
  - `/home/deploy/.openclaw/workspace/skills/xiaoao-home-appliance-market-analysis/SKILL.md`
  - `/home/deploy/.openclaw/workspaces/xiaoao-home-appliance-market-analyst/skills/xiaoao-home-appliance-market-analysis/SKILL.md`
  - `/home/deploy/.openclaw/workspaces/xiaoao-home-appliance-market-analyst/AGENTS.md`

部署结果：

- `scripts/hotfix-api.sh dev`：完成。
- 205 本机 `http://127.0.0.1:8000/readyz`：`{"status":"ready","database":"ok"}`。
- 远端编译检查：`python -m compileall app/cli/catforge_analyst.py app/services/core3_real_data/analyst/competitor_answer.py` 通过。
- 205 公网 `:8000` 当前不可直连，服务以服务器本机和反向代理路径为准；本任务验收使用 205 容器内 CLI 和本机 readyz。

## 3. 65E7Q E2E 命令

飞书报告生成：

```bash
docker compose -f docker-compose.cloud.yml --env-file .env exec -T api \
  python -m app.cli.catforge_analyst competitor-set \
  --query 65E7Q \
  --product-category tv \
  --batch-id latest \
  --limit 10 \
  --format json \
  --answer-style xiaoao \
  --with-report feishu-doc \
  --top-n 3 \
  --max-chat-chars 600
```

Markdown 报告结构检查：

```bash
docker compose -f docker-compose.cloud.yml --env-file .env exec -T api \
  python -m app.cli.catforge_analyst competitor-set \
  --query 65E7Q \
  --product-category tv \
  --batch-id latest \
  --limit 10 \
  --format json \
  --answer-style xiaoao \
  --with-report markdown \
  --top-n 3 \
  --max-chat-chars 600
```

小奥非卡片入口：

```bash
docker compose -f docker-compose.cloud.yml --env-file .env exec -T api \
  python -m app.cli.catforge_analyst competitor-set \
  --query 65E7Q \
  --product-category tv \
  --batch-id latest \
  --limit 10 \
  --format text \
  --answer-style xiaoao \
  --with-report none \
  --top-n 3 \
  --max-chat-chars 600
```

小奥飞书卡片入口边界：

```bash
docker compose -f docker-compose.cloud.yml --env-file .env exec -T api \
  python -m app.cli.catforge_analyst competitor-set \
  --query 65E7Q \
  --product-category tv \
  --batch-id latest \
  --limit 10 \
  --format text \
  --answer-style xiaoao \
  --with-report markdown \
  --top-n 3 \
  --max-chat-chars 600 \
  --feishu-chat-id oc_ca_g08_invalid \
  --feishu-reply-message-id om_ca_g08_invalid \
  --feishu-card-idempotency-key ca-g08-invalid \
  --feishu-card-only
```

## 4. 飞书报告

- 状态：`created`
- 链接：`https://my.feishu.cn/docx/P8MAds7qToUJWGxOwXQc12ohnLt`

## 5. Top 3 与看板一致性

目标 SKU：`TV00029112`，海信 65E7Q。

Top 3 输出顺序：

| 顺序 | 竞品 | SKU | 角色 | 综合分 | 锚点可替代性 | 替代压力 |
| ---: | --- | --- | --- | ---: | ---: | --- |
| 1 | 创维 65A7H PRO | TV00029936 | 首选直接竞品 | 0.9189 | 13/15 | 价值替代压力 9/10 |
| 2 | TCL 65Q9L PRO | TV00027801 | 强配置对标竞品 | 0.8995 | 13/15 | 价值替代压力 9/10 |
| 3 | 华为 VISION智慧屏 5 PRO 65 | TV00028909 | 价格下探分流竞品 | 0.9043 | 13/15 | 下探分流压力 9/10 |

一致性检查：

- `top_competitors` 顺序：创维 65A7H PRO、TCL 65Q9L PRO、华为 VISION智慧屏 5 PRO 65。
- `dashboard_payload.competitors` 顺序：创维 65A7H PRO、TCL 65Q9L PRO、华为 VISION智慧屏 5 PRO 65。
- 非卡片 `--format text` 与 `--format json --with-report none` 的 `short_answer` 完全一致。
- 短摘要长度：259 字；飞书报告版短摘要长度：313 字。
- 短摘要不包含 `CLI`、`CatForge`、`Mxx`、`BF_`、`TG_`、`TASK_`、`JSON`、`stderr`、`stdout`。
- 飞书卡片 JSON 大小：13,221 bytes，不包含内部 code。

说明：输出顺序按当前竞品角色和选择门槛形成，第二和第三名的原始 `competitor_score` 非严格降序，但短摘要、看板、卡片和报告顺序一致。

## 6. 报告结构验收

Markdown 报告长度：38,633 字符。

`## 二、分析过程` 下固定维度目录全部存在：

- `### 2.1 综合评分总览`
- `### 2.2 购买池比较`
- `### 2.3 价值战场比较`
- `### 2.4 用户任务比较`
- `### 2.5 目标客群比较`
- `### 2.6 关键价值锚点可替代性比较`
- `### 2.7 替代压力比较`
- `### 2.8 市场验证比较`
- `### 2.9 候选池与未选原因附录`

检查结果：

- 每个主维度都能横向覆盖本品和 Top 3。
- `候选池与未选原因附录` 只出现在 2.9。
- 关键价值锚点章节包含锚点可替代性口径。
- 替代压力章节包含压力类型和压力分。
- 市场验证章节存在。
- 报告正文不包含 `BF_`、`TG_`、`TASK_`、`CLI`、`CatForge`、`stderr`、`stdout`。

## 7. 小奥入口验收

非卡片入口：

- `--format text --answer-style xiaoao --with-report none` 输出短摘要。
- 输出与结构化 JSON `short_answer` 一致。
- 输出 Top 3 顺序与看板、卡片和报告一致。

飞书卡片入口：

- 使用 `--feishu-card-only` 后 stdout 优先输出卡片发送状态。
- 使用无效会话/消息 ID 验证失败边界时，stdout 为：`飞书卡片发送失败：当前消息 ID 不可回复或已失效。`
- 失败状态非空，且未暴露 scope、配置路径、堆栈、命令、原始 stdout/stderr 或内部字段。

## 8. 本地验证说明

本地直接运行 `catforge_analyst competitor-set` 时默认连接 SQLite，当前 SQLite 缺少 `core3_sku_claim_fact_profile` 等 Core3 结果表，因此本地不作为 E2E 数据验收环境。

CA-G08 的真实数据验收以 205 API 容器内 CLI 结果为准。

## 9. 结论

CA-G08 验收通过：

- 205 API 已热修复并 ready。
- 65E7Q 飞书报告已生成。
- 小奥入口、短摘要、卡片 payload 和报告 Top 3 顺序一致。
- 报告按购买池、价值战场、用户任务、目标客群、关键价值锚点、替代压力、市场验证等维度横向比较本品和 Top 3。
- 飞书卡片入口失败边界为非空、脱敏的业务状态。
