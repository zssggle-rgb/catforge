# V5-G00 关闭回执

## 结论

`PASSED — V5-G01 ALLOWED`。

V5 修改需求、详细设计、验收矩阵和串行 Goal 调度已冻结并独立提交。G00 未修改运行代码、未写数据库、未修改 205。

## 冻结的核心变化

1. 反事实从“必须找到三个直接 SKU”扩展为 direct、同预算、品牌梯度、参数梯度、同宣称不同兑现、自身价格曲线、市场合成基线和高/低绩效组合；
2. 市场合成对照同时回答“没有这组价值”的基线和高/低绩效组合参照；
3. 主、辅、机会和用户观察战场均视为已进入，优化分为已有战场增强与 excluded 新战场拓展；
4. M11D 当前战场销量分配、战场内部转移、观察性 gross、挤占和 net 分账；
5. 价值成立与可量化分开，价格承接与销量承接分开，严格金额门禁失败保持为空；
6. PM 输出以亮点、用户价值账、市场参照和战场组合方案为主，不生成通用自证或工作清单。

## 证据与检查

- 原始讨论附件已回读；
- V4 需求、设计、schema、G08R1 和 205 G09 结果已复核；
- 205 可行性数字仅作为 G00 只读快照，G01 必须重放并固化 hash；
- `git diff --cached --check` 通过；
- 只精确提交本目录 5 个核心文件；
- 主提交：`792b69d0be27a6c5a79534a64906e35ec36d5088`。

## 已知边界

- G00 未证明每种反事实的线上可用性；由 G01 负责真实覆盖和 Gold Set；
- 市场合成是观察性方法，不自动升级为因果；
- 默认路由切换不在本任务链，未来 V5-G10 需用户单独授权。

## 下一步

允许创建 V5-G01：只读核验真实字段、覆盖率、entered/excluded 战场、合成 donor 和 cohort，生成可重放 manifest/hash；不得写运行代码或 205。
