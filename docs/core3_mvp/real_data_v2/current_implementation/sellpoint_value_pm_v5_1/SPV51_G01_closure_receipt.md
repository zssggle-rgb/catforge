# SPV51-G01 需求与设计冻结关闭回执

状态：completed

日期：2026-07-17

## 1. 冻结结论

用户卖点价值画像 V5.1 按以下原则冻结：

1. 正式竞品唯一消费当前 `published + current` 的 `competitor_profile_agent_snapshot_v2`，不再以旧 M12/M13/M14 形成正式候选；
2. 竞品画像完整候选全部保存，Top 3 只标优先级；市场、参数、战场和合成对照继续作为独立分析参考；
3. 门槛约束问题级结论强度，不控制候选是否继续分析；一个维度缺失只让该问题无结论；
4. 直接市场量价比较采用最低 1 个合法对照、周均量价、无需共同周度/平台的主结果合同；
5. 参数组比较不要求预设高中低；基础功能样本不足只是不分类，不触发全局复核；
6. 市场合成与严格 WTP 保留为增强层，不再阻断普通量价、价值项、SKU 或版本；
7. 分析状态统一为 `conclusion_available / partial_conclusion / no_conclusion / invalid`，只有结构和完整性 invalid 才阻断版本；
8. 完整回归、覆盖率、性能和三类总评审集中到 SPV51-G13，各开发 Goal 只跑专项测试和必要回归。

## 2. 任务链

任务拆为 SPV51-G01—G18。开发、集成、部署、TV 单 SKU、AC 单 SKU、全量 draft 和发布准备审计相互独立。任何 review、publish、current 均不在当前授权范围。

## 3. 工作区保护

- 本 Goal 只新增 `sellpoint_value_pm_v5_1` 目录内 4 份文档；
- 未修改运行代码、数据库或 205；
- 既有工作区修改使用命名 stash `pre-spv-v5.1-workspace-20260717` 可恢复保存，不执行删除、reset 或 clean；
- 后续 Goal 只精确暂存本任务文件。

## 4. 验证

- 三份合同的权威源、低门槛、状态聚合、发布边界和 Goal 顺序一致；
- 文档 diff check 通过；
- SPV51-G02 可在干净工作树上开始只读基线审计。
